<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <<%- include('partials/head') %>
    <style>
        :root {
            --primary-dark: #0a192f;
            --primary-medium: #172a45;
            --primary-light: #303f60;
            --accent-color: #64ffda;
            --text-primary: #e6f1ff;
            --text-secondary: #8892b0;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
           
        }
        @font-face {
	font-family: 'dan';
	src: url('font/danstevis.otf') format('woff2');
  }
  @font-face {
	font-family: 'rezvan';
	src: url('font/Rezvan.ttf') format('woff2');
  }


        body {
            font-family:dan ;
            background-color: var(--primary-dark);
            color: var(--text-primary);
            line-height: 1.6;
            padding: 20px;
        }
        
        .back-btn {
            display: inline-block;
            color: var(--accent-color);
            text-decoration: none;
            margin-bottom: 1rem;
            font-size: 1rem;
            transition: all 0.3s;
        }
        
        .back-btn:hover {
            color: var(--text-primary);
        }
        
        .profile-card {
            max-width: 600px;
            margin: 0 auto;
            background-color: var(--primary-medium);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        
        .profile-header {
            background-color: var(--primary-light);
            padding: 2rem;
            text-align: center;
            position: relative;
        }
        
        .profile-name {
            font-size: 1.5rem;
            margin-bottom: 0.3rem;
        }
        
        .profile-title {
            color: var(--accent-color);
            font-size: 1rem;
        }
        
        .profile-body {
            padding: 2rem;
        }
        
        .info-item {
            display: flex;
            margin-bottom: 1.5rem;
            align-items: center;
        }
        
        .info-icon {
            width: 40px;
            height: 40px;
            background-color: var(--primary-light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 1rem;
            color: var(--accent-color);
            flex-shrink: 0;
        }
        
        .info-content {
            flex-grow: 1;
        }
        
        .info-label {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin-bottom: 0.2rem;
        }
        
        .info-value {
            font-size: 1.1rem;
        }
        
        .action-btns {
            display: flex;
            justify-content: space-between;
            margin-top: 2rem;
        }
        
        .btn {
            display: inline-block;
            padding: 0.6rem 1.5rem;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s;
            text-align: center;
        }
        
        .edit-btn {
            background-color: var(--accent-color);
            color: var(--primary-dark);
        }
        
        .edit-btn:hover {
            background-color: transparent;
            color: var(--accent-color);
            outline: 1px solid var(--accent-color);
        }
        
        .home-btn {
            background-color: var(--primary-light);
            color: var(--text-primary);
        }
        
        .home-btn:hover {
            background-color: var(--accent-color);
            color: var(--primary-dark);
        }
        
        @media (max-width: 768px) {
            .profile-card {
                margin: 0;
            }
            
            .profile-header {
                padding: 1.5rem;
            }
            
            .profile-body {
                padding: 1.5rem;
            }
            
            .action-btns {
                flex-direction: column;
                gap: 1rem;
            }
            
            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <a href="/" class="back-btn">
        <i class="fas fa-arrow-right"></i>
        بازگشت به صفحه اصلی
    </a>
    
    <div class="profile-card">
        <div class="profile-header">
            <h2 class="profile-name">نام و نام خانوادگی کاربر</h2>
            <p class="profile-title">پروفایل دانش آموز</p>
        </div>
        
        <div class="profile-body">
            <div class="info-item">
                <div class="info-icon">
                    <i class="fas fa-user"></i>
                </div>
                <div class="info-content">
                    <div class="info-label">نام کامل</div>
                    <div class="info-value">حسین مختاری</div>
                </div>
            </div>
            
            <div class="info-item">
                <div class="info-icon">
                    <i class="fas fa-phone"></i>
                </div>
                <div class="info-content">
                    <div class="info-label">شماره تلفن</div>
                    <div class="info-value">09999562748</div>
                </div>
            </div>
            
            <div class="info-item">
                <div class="info-icon">
                    <i class="fas fa-graduation-cap"></i>
                </div>
                <div class="info-content">
                    <div class="info-label">پایه تحصیلی</div>
                    <div class="info-value">نهم</div>
                </div>
            </div>
            
            <div class="action-btns">
                <a href="/change" class="btn edit-btn">
                    <i class="fas fa-edit"></i> ویرایش اطلاعات
                </a>
                <a href="/" class="btn home-btn">
                    <i class="fas fa-home"></i> صفحه اصلی
                </a>
            </div>
        </div>
    </div>
</body>
</html>